language: node_js
node_js:
  - 12.13.0
cache: npm

install:
  - cd client
  - npm ci
  - cd ../server
  - npm ci

script:

deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  on:
    branch: master